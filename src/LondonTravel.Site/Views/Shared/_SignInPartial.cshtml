@inject SiteOptions Options
@inject Microsoft.AspNetCore.Identity.SignInManager<LondonTravelUser> SignInManager
@{
    string displayName = User.GetDisplayName();
    string avatarUrl = User.GetAvatarUrl(Url.CdnContent("avatar.png", Options));
}
@if (SignInManager.IsSignedIn(User))
{
    <form asp-route="@SiteRoutes.SignOut" method="post" id="signOutForm" class="navbar-right">
        <ul class="nav navbar-nav navbar-right">
            <li>
                <a asp-route="@SiteRoutes.Manage" title="Manage your London Travel account" data-ga-label="Manage Account Navbar">
                    @displayName
                    <lazyimg alt="@displayName" title="@displayName" src="@avatarUrl" aria-hidden="true" />
                    <noscript>
                        <img alt="@displayName" title="@displayName" src="@avatarUrl" aria-hidden="true" />
                    </noscript>
                </a>
            </li>
            <li>
                <button type="submit" class="btn btn-link navbar-btn navbar-link" data-ga-label="Sign-Out Navbar">
                    Sign out
                    <i class="fa fa-sign-out" aria-hidden="true"></i>
                </button>
            </li>
        </ul>
    </form>
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li><a asp-route="@SiteRoutes.SignIn" rel="nofollow" title="Sign in to your London Travel account" data-ga-label="Sign-In Navbar">Sign in</a></li>
        <li><a asp-route="@SiteRoutes.Register" title="Register for a London Travel account" data-ga-label="Register Navbar">Register</a></li>
    </ul>
}
